pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven("https://repo.animecraft.fun/repository/maven-snapshots/")
    }
}

rootProject.name = "foodspect-back"
